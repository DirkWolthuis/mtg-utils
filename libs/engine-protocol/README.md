# engine-protocol

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build engine-protocol` to build the library.

## Running unit tests

Run `nx test engine-protocol` to execute the unit tests via [Vitest](https://vitest.dev/).
