# engine-util

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build engine-util` to build the library.

## Running unit tests

Run `nx test engine-util` to execute the unit tests via [Vitest](https://vitest.dev/).
